# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DemoTravisCI' do
    
  use_frameworks!

  pod 'SwiftyJSON'

  target 'DemoTravisCITests' do
    inherit! :search_paths
  end

end
